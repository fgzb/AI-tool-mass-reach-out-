requests>=2.28
PyYAML>=6.0
tzdata  # time zones on systems without a tz database (Windows, slim containers)
# Optional, for real MX / DNS lookups (otherwise DNS-over-HTTPS and A-record checks are used):
# dnspython>=2.4
